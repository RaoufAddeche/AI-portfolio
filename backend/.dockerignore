# Ne jamais copier le venv local (host) dans l'image : il casserait
# le venv construit par `uv sync` (shebangs/symlinks pointant vers l'hôte).
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.env
README.md
